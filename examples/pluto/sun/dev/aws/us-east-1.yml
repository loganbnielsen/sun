target:
  cluster_name: sun-dev-lbendtly
  base_domain: smoke-test.invalid
  terraform_var_file: ../../platform/infra/aws/smoke-test.tfvars

resources:
  app_db:
    omit: true

  events:
    omit: true

services:
  charge_svc:
    omit: true

  notify_worker:
    omit: true
