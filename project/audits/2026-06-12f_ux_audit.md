# Sun Developer Experience Audit — 2026-06-12 (fourth pass)

**Template:** `docs/audits/UX_AUDIT.md`
**Previous UX audit:** `project/audits/2026-06-12c_ux_audit.md`
**Method:** Full stage-by-stage pass against the current checkout (post-FRIC-004 merge, post-AUDIT-029 merge).

## Summary

| Stage | Result |
|-------|--------|
| Installation | PASS |
| Project creation | PASS |
| Local development | PASS |
| Cloud setup | PASS |
| First deploy | PASS |
| Shipping a change | PASS |
| Day-2 operations | PASS |
| Domain event flow | PASS |
| AI-agent working surface | PASS |

## Stage Results

**Stage 1 — Installation:** PASS. README.md Quickstart shows a single `curl | tar xz` install with `SUN_HOME` fallback for source checkout. Prerequisites listed clearly. No changes from previous audit.

**Stage 2 — Project Creation:** PASS. `sun new workspace` generates 26 files. Reproduction gate verified: `cd dogfood_post_fric004 && dune build` exits 0 with zero warnings. The FRIC-004 regression (`Option.filter` not in OCaml stdlib) is fully resolved — generated `main.ml` now uses `Option.bind (Sys.getenv_opt "POSTGRES_URL") (fun s -> if s = "" then None else Some s)`. Generated workspace has vendor symlinks to Sun framework and integrations; building from within the workspace directory picks up the Sun monorepo workspace and resolves all library dependencies.

**Stage 3 — Local Development:** PASS. EXP-017 (sun dev run undocumented) is resolved. TUTORIAL.md now has a full "Local iteration with `sun dev run`" subsection (lines ~80–108) explaining what it does, when to use it vs `sun up`, environment variables, and the native-process vs containerised tradeoff. README.md Quickstart references `sun dev run` with a one-line explanation. Both docs gate and reproduction gate pass.

**Stage 4 — Cloud Setup:** PASS. `sun cloud init` documented in TUTORIAL.md §"Cloud Infrastructure". Terraform modules present for AWS (`platform/infra/aws/`) and GCP (`platform/infra/gcp/`). No changes from previous audit.

**Stage 5 — First Deploy:** PASS. `sun up` / `sun deploy` documented; flags described in TUTORIAL.md. No changes from previous audit.

**Stage 6 — Shipping a Change:** PASS. TUTORIAL.md covers the change → deploy loop and `sun rollback`. No changes from previous audit.

**Stage 7 — Day-2 Operations:** PASS. `sun logs`, `sun migrate`, `sun status`, `sun new svc/worker/fn` all documented. No changes from previous audit.

**Stage 8 — Domain Event Flow:** PASS. `sun new event` documented; schema compatibility explained via `test/test_schemas.ml`. No changes from previous audit.

**Stage 9 — AI-Agent Working Surface:** PASS. Generated workspace has predictable edit points; framework concerns separated from business logic. No changes from previous audit.

## Findings

No open findings. All previously tracked findings are resolved:

| ID | Description | Status |
|----|-------------|--------|
| EXP-017 | `sun dev run` implemented but undocumented | RESOLVED (TUTORIAL.md §"Local iteration") |

The next highest EXP ticket ID used is EXP-017. Future findings should start at EXP-018.
