---
id: EXP-007
type: ux-finding
severity: high
source: hygiene/audits/2026-06-08_ux_audit.md
branch: EXP-007/rollback
worktree: ../sun-EXP-007-rollback
---

`sun rollback` command is missing.

**Description:** `sun rollback` does not exist. `sun migrate rollback` prints "not yet implemented" and exits 1. Recovering from a bad deploy requires knowing `kubectl rollout undo deployment/<name> -n <namespace>` plus Sun's naming convention.

**Impact:** An engineer who ships a breaking change must know kubectl and Sun's namespace/name conventions (`<workspace>-<domain>`, underscores to hyphens) to recover.

**Remediation:** Implement `sun rollback [<service>]` using `kubectl rollout undo` for each service discovered via `discover_services`, followed by `kubectl rollout status` to confirm recovery.

## Review — returned for revision
- `EXP-007/rollback` branch — implementation (`cmd_rollback.ml`, `main.ml`, `dune`, `README.md` changes) exists only as uncommitted working-tree files; `git diff main...EXP-007/rollback` is empty. Changes would be lost on checkout. Commit the work to the branch before re-submitting.

**Note:** The implementation itself is correct — all logic, error handling, and registration checks pass. Only the missing commit needs fixing.

## Review — automated checks passed
Build clean; diff non-empty (5 files, 134 insertions); `cmd_rollback.ml` implements `kubectl rollout undo` + `rollout status` per service with checked return codes; namespace convention correct; registered in `main.ml` and `dune`; `sun rollback` documented in README.
