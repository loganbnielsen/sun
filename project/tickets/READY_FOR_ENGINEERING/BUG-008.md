---
id: BUG-008
type: bug
severity: high
source: local-demo Sun_obs dogfood verification 2026-09-05
---

Local Loki container (`grafana/loki:3.0.0`, `ensure-loki.sh`) rejects every write with "at least 2 live replicas required, could only find 1" even though `common.replication_factor: 1` is set.

**Depends on:** None.

## Problem

This is the same failure mode BUG-006 described (that ticket was lost — deleted
by a batch ticket-state operation on 2026-09-05 without being fixed or moved to
DONE; see `project/tickets/DONE/` has no trace of it). BUG-006 diagnosed it
against `platform/infra/base/main.tf`'s Terraform Helm release; this instance is
the separate plain-Docker Loki container `ensure-loki.sh` starts for local
dev/tests/demo (`docker run grafana/loki:3.0.0 -config.file=/etc/loki/local-config.yaml`),
which never goes through that Terraform module at all — a different code path
with the same symptom.

Confirmed live:

```
$ curl -X POST http://localhost:3100/loki/api/v1/push -d '{"streams":[...]}
at least 2 live replicas required, could only find 1
HTTP 500
```

Investigation so far: the bundled `local-config.yaml` sets `common.replication_factor: 1`,
but `GET /config` shows `common.ring.replication_factor` (a *separate* field —
the generic ring struct embedded under `common:`, not the same as the
`common.replication_factor` convenience shortcut) defaults to 3 and is not
overridden by the shortcut. Mounting a corrected config with
`common.ring.replication_factor: 1` explicitly set (verified via `GET /config`
that the value actually changed to 1) did **not** fix the write failure —
the same "at least 2 live replicas" error persists with every relevant
`replication_factor` in the full `/config` dump reading 1 (only the unrelated
`query_scheduler.scheduler_ring` at 2, and the unused `storage_config.cassandra`
default at 3, remain non-1). This means the actual quorum check that's failing
is not reading any of the config fields inspected so far — something else
(possibly hardcoded, possibly a ring the write path consults that isn't
reachable via `/config`, possibly a real upstream Loki 3.0.0 bug) is still
enforcing effective RF=2+ on the write path.

Further investigation (same session, follow-up pass): tried the byte-for-byte
official upstream reference config
(`https://raw.githubusercontent.com/grafana/loki/v3.0.0/cmd/loki/loki-local-config.yaml`,
which adds an explicit `server.grpc_listen_port: 9096` and a `query_range`
cache block missing from our mount) — identical failure. Tried
`grafana/loki:2.9.0` with the same config — identical failure, ruling out a
3.0.0-specific regression. Tried `--network host` instead of the default
Docker bridge network — identical failure, ruling out WSL2/Docker Desktop
bridge networking as the cause. Retried the push 20 times over ~160s after
a fresh container reported `/ready` — every attempt failed identically,
ruling out a ring-stability startup race. Host and container clocks are in
sync (ruling out heartbeat-staleness from clock skew). Per
[grafana/loki#4717](https://github.com/grafana/loki/issues/4717), Loki's
actual quorum formula is `floor(max(num_ingesters, replication_factor)/2)+1`,
not the documented `floor(replication_factor/2)+1` — but with exactly 1
registered ingester and RF=1, that still evaluates to 1, not 2, so this
doesn't explain our case either.

At this point every avenue reachable without attaching a debugger or
instrumenting Loki's own source (to find which ring/count the write-path
quorum check in `pkg/distributor` is actually reading in this sandbox) has
been exhausted. This may be specific to the sandboxed container runtime
this was tested in rather than a config or version issue at all — a
plain-host (non-sandboxed) Docker environment should be tried next before
assuming it's a genuine upstream bug.

## Goal

A fresh `ensure-loki.sh` container accepts pushes and serves queries
immediately (no manual pod/container restart, no flaky first-N-requests
window) so demo/tutorial/e2e runs that depend on `LOKI_URL` are reliable.

## Remediation

1. Try this exact repro (stock `grafana/loki:3.0.0`, official upstream
   `local-config.yaml`, RF=1 already set) on a plain, non-sandboxed Docker
   host to isolate whether this is a sandbox-runtime artifact rather than
   a Loki config/version issue at all — everything version/network/config/
   timing-related has already been ruled out (see Problem section).
2. If it reproduces outside the sandbox too: attach a debugger or add
   temporary logging to a local Loki build's `pkg/distributor` write path
   to find which ring/count actually drives the quorum check here (not
   `common.ring`, not `ingester.lifecycler.ring` — both confirmed RF=1 via
   `/config` with no effect); consider filing upstream against
   grafana/loki with this exact repro if it's not already covered by an
   open issue.
3. `platform/local/config/loki-config.yaml` was created during this
   investigation but did not fix the issue and was removed — don't
   reintroduce it without a config change actually shown to help.
4. Validate: fresh container, immediate `curl -X POST .../loki/api/v1/push`
   succeeds (HTTP 204) with no retry/wait needed, and `examples/local-demo`'s
   Loki assertion passes end-to-end.

## Acceptance criteria

- A freshly started `ensure-loki.sh` container accepts a push immediately
  after `/ready` reports true, with no "live replicas" error.
- `examples/local-demo`'s "Loki: logs received" assertion passes reliably.
- `dune test` (or `run_tests.sh`'s e2e suite) passes its Loki assertion
  without a flake/retry loop worked around at the test level.
