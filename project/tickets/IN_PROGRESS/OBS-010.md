---
id: OBS-010
type: feature
severity: medium
source: observability-design-doc 2026-09-02
---

Add `sun open` command (logs/metrics/dashboard)

**Depends on:** OBS-011, OBS-012.

## Problem

`sun logs` already prints a copyable Grafana Explore URL before streaming
(`Sun_cli_logs.grafana_explore_url`, FEAT-023), but there's no standalone
way to jump straight to Grafana for metrics or the workspace dashboard —
`docs/architecture/observability-design.md`'s `sun open logs|metrics|dashboard`
commands don't exist yet.

`sun open dashboard` specifically needs OBS-011's dashboards to actually
exist first (nothing to deep-link to otherwise), and every `sun open`
variant needs OBS-012's backend-aware URL resolver instead of a flat
`--grafana-base-url`-style flag.

## Goal

A `sun open` command that opens (or prints, non-interactively) the right
Grafana view at workspace, domain, or service scope, reusing the URL-building
already proven in `Sun_cli_logs` and OBS-012's resolver.

## Acceptance criteria

- `sun open logs [domain[/service]]`, `sun open metrics [domain[/service]]`,
  `sun open dashboard [domain[/service]]` all work; omitting the scope opens
  the workspace-level view.
- `sun open dashboard [domain[/service]]` deep-links into OBS-011's actual
  provisioned dashboards (workspace overview when no scope given; the
  service template with `$domain`/`$service` preset via URL query params
  when scoped) — not a generic Grafana home-page link.
- Base URL for all three comes from OBS-012's resolver; when it returns "no
  generated URL for this backend" (`external`), print that note instead of
  a broken link.
- Default behavior prints the URL and attempts to open it in the user's
  browser (best-effort; never fails the command if no browser is available
  in the environment — print the URL either way).
- `--links` (or equivalent flag) prints the raw URL only, no browser-open
  attempt — for scripting/copy-paste, matching the doc's example.
- Tests cover URL construction at all three scopes, including the
  `external`-backend no-URL case, without requiring a live Grafana
  instance.
